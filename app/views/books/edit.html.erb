<h1>Editing Book</h1>

<%= form_with model:@book, local: true do |f| %>
  <h4>>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit "Update Book" %>
<% end %>

<p>
  <%= link_to "Show", books_path %> |
  <%= link_to "Back", book_path(@book) %>
</p>